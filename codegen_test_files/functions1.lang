struct Point {
    int x;
    int y;
}
def Point copyPoints(Point[] p) {
    return Point(p[0].x+p[1].x, p[0].y+p[1].y);
}

def void main(){
    //Point p = Point(2,3);
    Point[] ps = Point[2];
    ps[0] = Point(2,3);
    ps[1] = Point(3,3);
    writeInt(ps[0].x);
    Point res = copyPoints(ps);
    writeInt(res.x);
}
